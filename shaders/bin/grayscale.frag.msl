#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct main0_out
{
    float4 out_var_SV_Target [[color(0)]];
};

struct main0_in
{
    float4 in_var_COLOR0 [[user(locn0)]];
    float2 in_var_TEXCOORD0 [[user(locn1)]];
};

fragment main0_out main0(main0_in in [[stage_in]], texture2d<float> u_texture [[texture(0)]], sampler u_sampler [[sampler(0)]])
{
    main0_out out = {};
    float4 _30 = u_texture.sample(u_sampler, in.in_var_TEXCOORD0) * in.in_var_COLOR0;
    float _31 = _30.x;
    float _37 = ((_31 * 0.2989999949932098388671875) + (_30.y * 0.58700001239776611328125)) + (_31 * 0.114000000059604644775390625);
    out.out_var_SV_Target = float4(_37, _37, _37, _30.w);
    return out;
}

