include "common.fbs";

namespace fbs;      // 定义命名空间
// 心跳包
table ClientHeartbeat {
}

table ClientScene {
    come:bool;
    map_id:uint32;
    character:Character;
}

table ClientCharacterMove {
    movement:Movement;
}

table ClientVisibleMob{
    mob_ids:[uint32];
}