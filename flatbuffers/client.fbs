include "common.fbs";

namespace fbs;      // 定义命名空间
// 心跳包
table ClientHeartbeat {
}

table ClientScene {
    come:bool;
    scene_id:uint32;
}

table ClientCharacterMove {
    movement:Movement;
}

table ClientMobMove {
    mob_id:uint32;
    movement:Movement;
}