include "common.fbs";

namespace fbs;      // 定义命名空间

table ServerHeartbeat {
}

table ServerScene {
    scene_host:uint64;
    scene_id:uint32;
    your_id:uint64;
    
    players:[Player];
}

table ServerCharacterMove {
    movement:Movement;
    client_id:uint64;
}

table ServerMobMove {
    mob_id:uint32;
    movement:Movement;
}